---
name: plantree-workflow
description: 当用户需要从总目标生成、查看、编辑、裁剪或导出任务树执行提示时，使用 PlanTree 的本地 MCP 工具和交互 UI。适用于将线性计划转为人工确认后可复制到 Codex 的树状计划。
---

# PlanTree 工作流

当用户希望管理任务树或查看任务树计划状态时，使用 PlanTree MCP 工具。

## 工具顺序

1. 用户给出新总目标时，调用 `build_planner_prompt`，按照返回提示词生成严格 TaskTree JSON；不要执行任务。
2. 调用 `import_task_tree` 导入 JSON，再调用 `render_plan_tree`，让用户在 Codex 侧栏打开任务树。PlanTree 不提供内嵌任务树。
3. 用户通过 UI 或对话提出添加、改写、展开、排序或裁剪时，调用对应编辑工具；以最新快照为准。
4. 告知用户可在侧栏编辑并点击“复制执行文件”。按钮会把包含完整剩余任务链的 `plantree-prompt.md` 文件对象放入系统剪贴板。
5. 用户把该文件粘贴到目标 Codex 对话并发送后，按照文件中的顺序实际执行每项任务；只有任务完成且验收通过后才进入下一项。
6. 任务失败、被阻塞、需要批准或验收未通过时停止，并向用户说明。
7. 用户要求撤回最近一次成功编辑时调用 `undo_last_edit`；用户明确确认重置后调用 `reset_demo`。

## 响应约束

- 以 MCP 工具返回的最新结构化快照为准。
- `render_plan_tree` 自行管理回环地址上的本地服务；不要要求用户运行终端命令或复制本地 URL。
- PlanTree 不传输对话标识、不唤醒 Codex 回合，也不创建后台执行请求；`plantree-prompt.md` 是唯一交接载体。
- 侧栏链接无法打开时，继续基于数据工具的结构化结果说明树、有效性、影响范围和审计记录，并明确报告侧栏入口故障。
- 不得调用 `simulate_execution` 代替真实任务执行。
- 不得仅复述 `task.prompt` 后就标记完成；必须完成实际工作并进行验收。
- 不得执行 `skipped` 节点、越过当前节点或虚假调用 `complete_task`。
- 不得要求用户逐项复制提示词；一次点击必须生成并复制一个包含完整剩余队列的 Markdown 文件对象。
