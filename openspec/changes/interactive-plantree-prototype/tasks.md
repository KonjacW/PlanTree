## 1. 插件与 MCP 基础

- [x] 1.1 使用插件脚手架创建带 `.codex-plugin/plugin.json`、技能目录、MCP 服务目录和 UI 资源目录的本地插件包。
- [x] 1.2 配置本地 stdio MCP 服务及其开发测试入口，不创建独立网页服务器或外部浏览器启动流程。
- [x] 1.3 定义 PlanTree skill 的触发范围与工具使用顺序，使 Codex 能在任务规划编辑场景中发现该插件。
- [x] 1.4 添加本地 marketplace 条目，并验证插件可安装、启用和卸载。

## 2. 任务树领域模型

- [x] 2.1 实现计划、节点、节点类型、状态、显式依赖、计划版本和不可变审计条目的领域类型。
- [x] 2.2 编写预置缺陷修复演示数据及其初始有效性测试。
- [x] 2.3 实现不可变计划读写、节点查询、后代遍历和稳定顺序管理。
- [x] 2.4 实现添加、改写、展开、裁剪和撤销命令，并测试根节点保护、叶节点保护和依赖循环拒绝。

## 3. 确定性重规划、验证与模拟执行

- [x] 3.1 实现影响范围分析器，计算编辑节点、未裁剪后代和反向依赖闭包，并测试无关分支不变性。
- [x] 3.2 实现基于节点类型模板的确定性模拟规划器，覆盖改写、展开、添加和裁剪。
- [x] 3.3 实现依赖同步与计划有效性检查，处理已裁剪依赖、失效节点、父子循环、依赖循环和执行前置条件。
- [x] 3.4 实现可执行叶节点的单步模拟执行和阶段状态汇总。
- [x] 3.5 实现只追加审计日志，覆盖编辑、影响计算、重规划、验证、执行、撤销和重置。

## 4. MCP 服务与无 UI 降级

- [x] 4.1 实现创建或加载演示会话工具，返回完整结构化计划快照、验证结果和初始化日志。
- [x] 4.2 实现编辑节点、模拟执行、撤销和重置工具，使每个工具都经由会话控制器完成状态转换。
- [x] 4.3 为每个数据工具定义稳定输入输出 schema、结构化结果和人类可读摘要，并测试在未渲染 UI 时的完整性。
- [x] 4.4 实现唯一的 `render_plan_tree` 渲染工具，使其接收快照并关联 MCP App UI 资源。
- [x] 4.5 编写 MCP 集成测试，验证数据工具不会附带 UI、渲染工具会返回 UI 资源、以及全部工具不发起外部请求。

## 5. Codex 小窗组件

- [x] 5.1 构建 MCP App UI 组件，呈现任务树层级、节点状态、依赖提示和选中节点详情。
- [x] 5.2 通过 MCP Apps bridge 调用 MCP 工具，实现添加、改写、展开、裁剪、撤销、模拟执行和重置。
- [x] 5.3 让 UI 在工具结果到达后以服务端快照刷新，并仅在组件中保存选中节点、展开状态和表单草稿。
- [x] 5.4 配置小窗资源的最小权限与内容安全策略，并验证它不会打开外部浏览器。
- [x] 5.5 编写组件测试，覆盖编辑反馈、失效依赖阻断、审计链路和无效性状态展示。

## 6. 端到端插件验证与演示

- [ ] 6.1 在本地 marketplace 安装插件，并在 Codex 新会话中验证 skill 触发、数据工具调用和小窗渲染。
- [ ] 6.2 走查演示路径：改写调查节点、展开修复节点、裁剪依赖分支、撤销、模拟执行和重置。
- [ ] 6.3 验证 UI 不可用时，Codex 仍可仅用 MCP 工具完成同一编辑与审计流程。
- [ ] 6.4 运行静态检查、领域单元测试、MCP 集成测试和 UI 组件测试，并记录全部结果。
- [ ] 6.5 更新插件使用说明与演示脚本，明确 Codex 内嵌小窗、模拟规划器和非真实代码执行的范围。
