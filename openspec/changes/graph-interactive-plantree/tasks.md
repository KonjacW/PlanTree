## 1. 图形依赖与模型适配

- [x] 1.1 在 `plugins/plantree/ui` 添加 `@xyflow/react` 运行时依赖，并确认 React 19、Vite 生产构建和 MCP App 资源均可加载。
- [x] 1.2 编写图模型适配器的失败测试，覆盖稳定分层位置、跳过节点过滤、父子实线和依赖虚线。
- [x] 1.3 实现从 `PlanSnapshot` 到 React Flow 节点与边的纯适配器，并实现基于 `childIds` 的稳定根向下布局。
- [x] 1.4 为状态、阻塞、根节点和可执行节点实现自定义图节点卡片，并添加组件渲染测试。

## 2. 图形画布与视觉交互

- [x] 2.1 编写画布失败测试，覆盖初始适配视图、缩放平移、框选以及 Shift/Ctrl/Cmd 多选行为。
- [x] 2.2 将现有列表树主区域替换为受控 React Flow 画布，同时保留既有 `ToolCaller`、版本注入和状态反馈协议。
- [x] 2.3 实现节点本地视觉位置拖动，并测试拖动不会调用 `move_node` 或修改任务树业务快照。
- [x] 2.4 完成紧凑画布、节点、边图例、操作条和窄宽度降级样式，确保 MCP PiP 与本地 Web 共用样式。

## 3. 节点提示词详情

- [x] 3.1 编写提示词派生函数的失败测试，覆盖节点目标、类型、状态、父节点、子节点、依赖和一致性说明。
- [x] 3.2 实现不写入快照、也不发起网络请求的单节点提示词生成器。
- [x] 3.3 实现可复制、可关闭、可键盘聚焦的提示词抽屉或浮层，并测试 `P` 开关、多选隐藏和节点删除后的自动关闭。

## 4. 快捷键、确认与冲突恢复

- [x] 4.1 编写快捷键失败测试，覆盖 `P`、`Enter`、`E`、`Delete`、`Ctrl/Cmd+Z`、`Esc`、`?` 以及未实现 `F` 的约束。
- [x] 4.2 实现仅在图形窗口活动且非可编辑元素聚焦时生效的快捷键控制器，并保留所有操作的点击入口。
- [x] 4.3 实现单选/多选裁剪确认、根节点排除和逐项提交；测试冲突或失败时停止后续提交。
- [x] 4.4 扩展服务端快照与版本冲突恢复逻辑，保留仍存在节点的选择和视觉位置，清理失效节点的选择与提示词状态。

## 5. 验证与文档

- [x] 5.1 运行 UI 单元测试、类型检查和生产构建，并修复本变更导致的失败。
- [x] 5.2 运行服务端完整测试，确认图形 UI 未改变 MCP/HTTP 版本保护和持久化任务树行为。
- [x] 5.3 在本地 Web 走查图形画布：框选、视觉拖动、提示词、全部快捷键、裁剪确认、冲突恢复与重置。
- [ ] 5.4 在 Codex MCP PiP 走查同一图形交互；若宿主未渲染小窗，验证数据工具仍能无 UI 地完成相同编辑流程。
- [x] 5.5 更新 `plugins/plantree/README.md`，说明图形交互、快捷键、提示词为本地派生信息、拖动不改变任务结构，以及不支持的交互范围。

## 6. 人工提示词与重做

- [x] 6.1 先编写集中监测点失败测试：纯函数覆盖人工/自动提示词选择、过期判断和空白校验；领域覆盖保存与清除字段和版本；会话覆盖撤销、重做、新变更清空重做和重置清空历史；协议各保留一组 MCP/HTTP 映射；UI 用一组集成测试覆盖完整编辑流程，避免跨层重复验证相同规则。
- [x] 6.2 扩展 `PlanNode` 与提示词领域编辑：增加可选 `customPrompt`、`customPromptBaseVersion`，为 `edit_node` 增加 `prompt` 操作，实现非空人工提示词保存、恢复自动生成、节点与计划版本更新和审计，并保持旧 JSON 无迁移加载。
- [x] 6.3 在 `DemoSession` 实现会话级重做栈，支持连续撤销与重做、撤销后新变更清空重做、重置清空撤销与重做，不增加数据库或独立历史系统。
- [x] 6.4 扩展轻量协议映射：注册 `redo_last_edit` MCP 工具、增加 `/api/redo`、使 HTTP `ToolCaller` 支持重做，并让人工提示词保存继续复用 `edit_node`；所有写操作继续携带 `expectedVersion`。
- [x] 6.5 在现有提示词浮层实现只读、编辑、保存、取消、复制、恢复自动生成、未保存关闭确认和人工提示词过期提示，并增加 `Ctrl/Cmd+Y`；Web 与 PiP 继续复用同一组件且不改变现有任务图布局。
- [x] 6.6 集中运行 UI 测试、类型检查、生产与 MCP 构建、服务端测试与构建和 OpenSpec strict validate，并更新 README 中人工提示词持久化、过期提示和重做说明。
